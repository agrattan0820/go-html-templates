{
  "name": "go-html-templates",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild ./src/scripts/** ./src/styles/** --bundle --outdir=dist --watch",
    "build": "esbuild ./src/scripts/** ./src/styles/** --bundle --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
